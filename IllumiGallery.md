请你根据以下需求，生成一个完整的“二次元壁纸展示纯静态网页”的设计与实现方案（包括页面结构、UI 设计、交互细节、文件组织、响应式布局方案、动画效果、组件划分、展示方式等）。请确保所有细节清晰具体，适合直接用于生成代码或继续扩展成项目。需求如下：

【项目目标】

创建一个纯静态网页，主题为 二次元风格壁纸展示站，用于展示我收藏的动漫壁纸。

【核心功能需求】
1. 壁纸批量导入能力

能让我一次选择多张图片，自动加入壁纸集（本地文件，不依赖后台）。

支持 JPG / PNG / WebP。

图片加入后自动记录在静态数据结构中（如 JSON / JS 数组）。

2. 自动识别横屏 vs 竖屏

系统读取图片尺寸，根据比例自动分类：

横屏壁纸（宽 > 高）

竖屏壁纸（高 ≥ 宽）

识别后自动放入不同的页面区块展示。

3. 首页展示逻辑
首页横屏区：电影投影风展示

横屏壁纸在首页以 投影幕布效果方式展示：

黑暗影院风格背景

居中大图

带投影光晕、幕布感阴影

左右切换按钮（下一张 / 上一张）

可自动播放轮播（可开关）

图片切换时带 淡入 / 缓慢滑动 特效

首页竖屏区：瀑布流展示

竖屏壁纸在首页以下方式展示：

Pinterest 风格瀑布流布局

轻微缩放 hover 动画

点击可弹出预览（lightbox）

保持二次元 UI 美感（柔和蓝紫系 + 发光边框）

4. 壁纸详情展示（Lightbox 模式）

点击任何壁纸，可进入全屏预览：

全屏透明黑背景

图片自动适配居中

左右箭头切换

支持键盘左右键操作

点击外部关闭

动画柔和淡入

5. 视觉风格（AI 关键指令）

整体为 二次元 / Galgame / UI 二次元面板混合风格

使用：

霓虹蓝 + 淡紫色发光边框

半透明面板

干净圆角

柔和渐变背景

动画风格轻盈、有科技感

6. 响应式要求

PC：电影投影效果大屏展示

平板：保持横图适配

手机：

横图缩小比例展示

瀑布流两列变一列

轻量交互保证流畅

7. 技术要求（适合纯静态页）

纯 HTML + CSS + JS

无后端、无数据库

所有数据存放在一个 JSON 文件或 JS 数组里

可直接部署到 GitHub Pages

尽量减少依赖（只可选用轻量 JS 库，如 Masonry，可选）

8. 可选增强功能

（请 AI 视情况自动补充设计方案）

壁纸收藏与标签（存在 localStorage）

搜索壁纸（按Tag/名称）

自动生成预览缩略图

二次元风格 Loading 动效

分享到社交媒体按钮

请根据以上完整细则输出：

静态网页的整体结构

所需组件列表

页面 UI 布局草图（文字描述即可）

前端代码生成方案

动画与交互逻辑

文件树结构

可直接用于编码的最终指令或代码